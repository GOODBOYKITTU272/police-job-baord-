// Recommended Logo Dimensions for Right Logo (Siddipet Police / Suraksha Siddipet)

module.exports = {
  // The CSS renders the logo at 60px height. 
  // We recommend 2x or 3x resolution for crispness on Retina/High-DPI displays.
  recommendedHeightPx: 180,
  
  // The width should be proportional. Assuming the logo is slightly taller than it is wide:
  recommendedWidthPx: 150,
  
  // CRITICAL REQUIREMENT:
  // The image must be cropped TIGHTLY to the edges of the logo graphic.
  // There should be NO transparent padding/empty space around the logo itself.
  // If there is transparent padding, the logo will appear small on the website.
  padding: "0px (Tightly cropped)"
};
